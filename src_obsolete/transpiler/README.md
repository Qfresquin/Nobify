The files in this directory are frozen legacy code.

They preserve the old structural Event IR contract that existed before the
semantic Event IR rewrite became the main architecture.

These files are retained only as historical reference while the evaluator and
future downstream consumers migrate to the new canonical contract in
`src_v2/transpiler/event_ir.h`.
