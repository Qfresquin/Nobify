# Evaluator Event IR Contract (Rewrite Draft)

Status: Draft rewrite. This document is intended to describe the evaluator's output guarantees toward Event IR.

## Rewrite Boundary

This file is a placeholder for the evaluator-output slice of the rewrite.

Current rule:
- use `docs/Evaluator/` as historical reference,
- treat `src_v2/transpiler/event_ir.*` and `docs/Evaluator/event_ir_v2_spec.md` as legacy reference material,
- describe evaluator-side output guarantees here,
- keep this file subordinate to `evaluator_v2_spec.md`.

## Planned Sections

- Event emission boundary
- Append-only guarantees
- Origin metadata rules
- Event payload ownership
- Evaluator-side event families and invariants
