    {"add_compile_definitions", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_dependencies", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_compile_options", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_custom_command", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_custom_target", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_definitions", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_executable", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_library", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_link_options", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.build-graph", "command already freezes into downstream build graph or supported target metadata", NULL},
    {"add_subdirectory", EGD_PACK_ADD_SUBDIRECTORY, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "pipeline.directory", "command already freezes into canonical directory traversal and downstream state", NULL},
    {"add_test", EGD_PACK_TESTING_META, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_TEST, "build-model.test", "command already freezes into downstream test registration state", NULL},
    {"block", EGD_PACK_FLOW_CONTROL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.control-flow", "command is resolved during configure evaluation and only survives through frozen downstream state", NULL},
    {"break", EGD_PACK_FLOW_CONTROL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.control-flow", "command is resolved during configure evaluation and only survives through frozen downstream state", NULL},
    {"build_command", EGD_PACK_HOST_IDENTITY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_HOST_ONLY, "evaluator.host-introspection", "command remains evaluator-only host introspection", NULL},
    {"build_name", EGD_PACK_HOST_IDENTITY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_HOST_ONLY, "evaluator.host-introspection", "command remains evaluator-only host introspection", NULL},
    {"cmake_host_system_information", EGD_PACK_HOST_IDENTITY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_HOST_ONLY, "evaluator.host-introspection", "command remains evaluator-only host introspection", NULL},
    {"cmake_file_api", EGD_PACK_FILE_API_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.file-api", "replay-domain foundation landed, but this evaluator-implemented command still needs explicit backend replay or reject coverage", "epic-c.file-api"},
    {"cmake_parse_arguments", EGD_PACK_ARGUMENT_PARSING, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.arguments", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"cmake_minimum_required", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.project", "command already freezes into canonical project or language setup state", NULL},
    {"cmake_language", EGD_PACK_CMAKE_LANGUAGE, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.cmake-language", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"cmake_path", EGD_PACK_CMAKE_PATH, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.cmake-path", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"cmake_policy", EGD_PACK_POLICY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.policy", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"configure_file", EGD_PACK_CONFIGURE_FILE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.configure-output", "command already freezes into canonical configure-time output state", NULL},
    {"continue", EGD_PACK_FLOW_CONTROL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.control-flow", "command is resolved during configure evaluation and only survives through frozen downstream state", NULL},
    {"create_test_sourcelist", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"cpack_add_component", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_PACKAGE, "build-model.package", "command freezes into supported CPack archive component packaging metadata for TGZ/TXZ/ZIP", NULL},
    {"cpack_add_component_group", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_PACKAGE, "build-model.package", "command freezes into supported CPack archive component grouping metadata for TGZ/TXZ/ZIP", NULL},
    {"cpack_add_install_type", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_PACKAGE, "build-model.package", "command freezes into supported CPack component metadata; interactive installer semantics remain outside archive backend scope", NULL},
    {"ctest_build", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local self-build ctest steps are part of the closed local CTest replay row and replay through the generated backend", NULL},
    {"ctest_configure", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local self-configure ctest steps are part of the closed local CTest replay row and replay through the generated backend", NULL},
    {"ctest_coverage", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local-only coverage dashboard collection is part of the closed local CTest replay row and replays through the generated backend", NULL},
    {"ctest_empty_binary_directory", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local empty-binary-directory steps are part of the closed local CTest replay row and replay through the generated backend", NULL},
    {"ctest_memcheck", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local-only memcheck dashboard execution is part of the closed local CTest replay row and replays through the generated backend", NULL},
    {"ctest_read_custom_files", EGD_PACK_CTEST, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "child-script custom-file ctest flows remain explicit backend rejects in C3", "epic-c.ctest-extended"},
    {"ctest_run_script", EGD_PACK_CTEST, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "ctest_run_script remains an explicit backend reject in local-only C3", "epic-c.ctest-extended"},
    {"ctest_sleep", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local ctest_sleep is part of the closed local CTest replay row and replays through the generated backend", NULL},
    {"ctest_start", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local ctest_start without checkout is part of the closed local CTest replay row and replays through the generated backend", NULL},
    {"ctest_submit", EGD_PACK_CTEST, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "dashboard submit and upload flows remain explicit backend rejects in local-only C3", "epic-c.ctest-extended"},
    {"ctest_test", EGD_PACK_CTEST, EGD_CLASS_PARITY_PASS, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "local ctest_test is part of the closed local CTest replay row and replays through the generated backend test domain", NULL},
    {"ctest_update", EGD_PACK_CTEST, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "update/dashboard VCS flows remain explicit backend rejects in local-only C3", "epic-c.ctest-extended"},
    {"ctest_upload", EGD_PACK_CTEST, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_TEST | EGD_PHASE_HOST_ONLY, "build-model.replay.test-driver", "dashboard upload flows remain explicit backend rejects in local-only C3", "epic-c.ctest-extended"},
    {"define_property", EGD_PACK_PROPERTY_SETTERS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.properties", "command already freezes into downstream property state", NULL},
    {"enable_language", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.project", "command already freezes into canonical project or language setup state", NULL},
    {"enable_testing", EGD_PACK_TESTING_META, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_TEST, "build-model.test", "command already freezes into downstream test registration state", NULL},
    {"endblock", EGD_PACK_FLOW_CONTROL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.control-flow", "command is resolved during configure evaluation and only survives through frozen downstream state", NULL},
    {"exec_program", EGD_PACK_EXECUTE_PROCESS, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "build-model.replay.process", "generic process replay is now typed downstream ownership, but still an explicit backend reject in C3", "epic-c.process-probes"},
    {"execute_process", EGD_PACK_EXECUTE_PROCESS, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "build-model.replay.process", "generic process replay is now typed downstream ownership, but still an explicit backend reject in C3", "epic-c.process-probes"},
    {"export_library_dependencies", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"file", EGD_PACK_FILE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.configure", "deterministic local configure filesystem and host-effect file() signatures replay through the generated backend; remote/process/probe-like variants remain explicit subcommand rejects", NULL},
    {"export", EGD_PACK_EXPORT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_EXPORT, "build-model.export", "command already freezes into canonical export records or supported export execution", NULL},
    {"FetchContent_Declare", EGD_PACK_FETCHCONTENT, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "build-model.replay.dependency-materialization", "FetchContent declarations feed the closed local dependency-materialization row, while unsupported providers, remotes, VCS, and custom flows remain explicit backend rejects", "epic-c.fetchcontent"},
    {"FetchContent_GetProperties", EGD_PACK_FETCHCONTENT, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.fetchcontent", "GetProperties only affects configure-time variables and remains evaluator-only in C3", NULL},
    {"FetchContent_MakeAvailable", EGD_PACK_FETCHCONTENT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.dependency-materialization", "local SOURCE_DIR and local-archive MakeAvailable flows are part of the closed dependency-materialization row", NULL},
    {"FetchContent_Populate", EGD_PACK_FETCHCONTENT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.dependency-materialization", "local SOURCE_DIR and local-archive Populate flows are part of the closed dependency-materialization row", NULL},
    {"FetchContent_SetPopulated", EGD_PACK_FETCHCONTENT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.dependency-materialization", "SetPopulated continues to feed the supported local dependency-materialization surface in C3", NULL},
    {"get_cmake_property", EGD_PACK_PROPERTY_WRAPPERS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"get_directory_property", EGD_PACK_PROPERTY_WRAPPERS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"find_file", EGD_PACK_FIND_PATHLIKE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.path-search", "command already freezes into downstream state after configure-time path search", NULL},
    {"find_library", EGD_PACK_FIND_PATHLIKE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.path-search", "command already freezes into downstream state after configure-time path search", NULL},
    {"find_path", EGD_PACK_FIND_PATHLIKE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.path-search", "command already freezes into downstream state after configure-time path search", NULL},
    {"find_package", EGD_PACK_FIND_PACKAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.package-discovery", "command already freezes into downstream package resolution state", NULL},
    {"find_program", EGD_PACK_FIND_PATHLIKE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.path-search", "command already freezes into downstream state after configure-time path search", NULL},
    {"get_filename_component", EGD_PACK_GET_FILENAME_COMPONENT, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.path-query", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"get_property", EGD_PACK_PROPERTY_QUERY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"get_source_file_property", EGD_PACK_PROPERTY_QUERY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"get_target_property", EGD_PACK_PROPERTY_QUERY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"get_test_property", EGD_PACK_PROPERTY_QUERY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.properties", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"include", EGD_PACK_INCLUDE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.include", "command already freezes into downstream state through canonical include evaluation", NULL},
    {"include_external_msproject", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"include_regular_expression", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"include_directories", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"include_guard", EGD_PACK_INCLUDE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.include", "command already freezes into downstream state through canonical include evaluation", NULL},
    {"install", EGD_PACK_INSTALL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_INSTALL, "build-model.install", "command already freezes into canonical install rules", NULL},
    {"install_files", EGD_PACK_INSTALL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_INSTALL, "build-model.install", "command already freezes into canonical install rules", NULL},
    {"install_programs", EGD_PACK_INSTALL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_INSTALL, "build-model.install", "command already freezes into canonical install rules", NULL},
    {"install_targets", EGD_PACK_INSTALL, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_INSTALL, "build-model.install", "command already freezes into canonical install rules", NULL},
    {"link_directories", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"link_libraries", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"list", EGD_PACK_LIST, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.list", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"load_command", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"load_cache", EGD_PACK_CACHE_LOADING, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.cache", "command already freezes into canonical cache-loading state", NULL},
    {"make_directory", EGD_PACK_FILE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.configure", "command now replays as a deterministic configure-time filesystem action in the generated backend", NULL},
    {"mark_as_advanced", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"math", EGD_PACK_MATH, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.math", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"message", EGD_PACK_MESSAGE, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.message", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"option", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"output_required_files", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"project", EGD_PACK_ADD_TARGETS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.project", "command already freezes into canonical project or language setup state", NULL},
    {"qt_wrap_cpp", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "replay.backlog.legacy-wrappers", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"qt_wrap_ui", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "replay.backlog.legacy-wrappers", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"remove", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"remove_definitions", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"return", EGD_PACK_CALLABLE_SCOPE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.callable-scope", "command is resolved during configure evaluation and only survives through frozen downstream state", NULL},
    {"separate_arguments", EGD_PACK_ARGUMENT_PARSING, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-query.arguments", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"set", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"set_directory_properties", EGD_PACK_PROPERTY_SETTERS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.properties", "command already freezes into downstream property state", NULL},
    {"set_property", EGD_PACK_PROPERTY_SETTERS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.properties", "command already freezes into downstream property state", NULL},
    {"set_source_files_properties", EGD_PACK_PROPERTY_SETTERS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.properties", "command already freezes into downstream property state", NULL},
    {"set_target_properties", EGD_PACK_PROPERTY_SETTERS, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.properties", "command already freezes into downstream property state", NULL},
    {"set_tests_properties", EGD_PACK_TESTING_META, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_TEST, "build-model.test", "command already freezes into downstream test registration state", NULL},
    {"site_name", EGD_PACK_HOST_IDENTITY, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_HOST_ONLY, "evaluator.host-introspection", "command remains evaluator-only host introspection", NULL},
    {"string", EGD_PACK_STRING, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.string", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"source_group", EGD_PACK_LEGACY_META, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE, "replay.backlog.legacy-meta", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"subdir_depends", EGD_PACK_LEGACY_META, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.legacy-meta", "command already freezes into downstream state or supported legacy metadata behavior", NULL},
    {"subdirs", EGD_PACK_LEGACY_META, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.legacy-meta", "command already freezes into downstream state or supported legacy metadata behavior", NULL},
    {"aux_source_directory", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.default-frozen", "command already freezes into downstream state without backend replay", NULL},
    {"target_compile_features", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_compile_definitions", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_compile_options", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_include_directories", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_link_directories", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_link_libraries", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_link_options", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"target_precompile_headers", EGD_PACK_TARGET_USAGE, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "replay.backlog.target-usage", "replay-domain foundation landed, but this evaluator-implemented command still needs explicit backend replay or reject coverage", "epic-a.target_precompile_headers"},
    {"target_sources", EGD_PACK_TARGET_USAGE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.target-usage", "command already freezes into downstream target usage or build inputs", NULL},
    {"try_compile", EGD_PACK_TRY_COMPILE, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.replay.probe", "try_compile now has typed downstream probe ownership, but generated probe execution remains an explicit backend reject in C3", "epic-c.try-apis"},
    {"try_run", EGD_PACK_TRY_RUN, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "build-model.replay.probe", "try_run now has typed downstream probe ownership, but generated probe execution remains an explicit backend reject in C3", "epic-c.try-apis"},
    {"unset", EGD_PACK_VARS, EGD_CLASS_EVALUATOR_ONLY, EGD_PHASE_CONFIGURE, "evaluator.frozen-semantic.variables", "command remains evaluator-only and affects generated output only through already-frozen downstream state", NULL},
    {"use_mangled_mesa", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.default-frozen", "command already freezes into downstream state without backend replay", NULL},
    {"utility_source", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.default-frozen", "command already freezes into downstream state without backend replay", NULL},
    {"variable_requires", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.default-frozen", "command already freezes into downstream state without backend replay", NULL},
    {"variable_watch", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "pipeline.default-frozen", "command already freezes into downstream state without backend replay", NULL},
    {"fltk_wrap_ui", EGD_PACK_EVAL_DEFAULT, EGD_CLASS_BACKEND_REJECT, EGD_PHASE_CONFIGURE | EGD_PHASE_BUILD, "replay.backlog.legacy-wrappers", "command is evaluator-implemented but still needs explicit downstream replay or reject coverage", "epic-backend-reject.default"},
    {"write_file", EGD_PACK_FILE, EGD_CLASS_PARITY_PASS, EGD_PHASE_CONFIGURE, "build-model.replay.configure", "command now replays as a deterministic configure-time text materialization action in the generated backend", NULL},
